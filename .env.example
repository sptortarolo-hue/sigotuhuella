# DATABASE_URL: PostgreSQL connection string.
# Required for both local development and Render deployment.
DATABASE_URL="postgresql://user:password@localhost:5432/sigotuhuella"

# JWT_SECRET: Secret key for generating auth tokens.
# Required. Change this to a strong random string in production.
JWT_SECRET="change-me-to-a-random-secret"

# ADMIN_EMAIL: Email address that will be auto-created as admin on first startup.
# Optional. If set, the server seeds an admin user with this email.
ADMIN_EMAIL="sptortarolo@gmail.com"

# ADMIN_PASSWORD: Password for the auto-created admin user.
# Only used if ADMIN_EMAIL is set. Defaults to "admin123456".
ADMIN_PASSWORD="admin123456"
